# Notes:
`game.html` follows the layout in the requirement **strictly** (no extra CSS)

`game_bootstrap_css.html` adds extra bootstrap classes to style the elements according to Bootstrap general style
